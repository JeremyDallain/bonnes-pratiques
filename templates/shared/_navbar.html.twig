<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('homepage') }}">BP</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="pe-3">
					<a href="{{ path('admin') }}" class="btn btn-sm btn-danger">Admin</a>
				</li>					
				<li class="pe-3">
					<a href="{{ path('article') }}" class="btn btn-sm btn-danger">Les articles</a>
				</li>					
			</ul>
			<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
				{% if app.user %}
					<li class="pe-3">
						{{ app.user.email }}
					</li>
					<li class="nav-item">
						<a href="{{ path('app_logout') }}" class="btn btn-sm btn-danger">Logout</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a href="#" class="nav-link">Inscription</a>
					</li>
					<li class="nav-item">
						<a href="{{ path('app_login') }}" class="btn btn-success">Login</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
